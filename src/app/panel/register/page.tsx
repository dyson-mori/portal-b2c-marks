import React from 'react';

import Register from './components/register';

// import { getAnswers } from './action';

export default async function Page() {
  // const { data, loading } = await getAnswers();

  return (
    <Register />
  );
};
